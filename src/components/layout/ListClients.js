const ListClient = (props) => {
  const clients = [];
  /* for (let i = 0; i < props.nombreDeClients; i++) {
    clients.push(
      <Client
        tempRestant={number}
        width={dimensions.width}
        height={dimensions.height}
      ></Client>
    );
  }*/
  return <div className="ListClient"></div>;
};

export default ListClient;
